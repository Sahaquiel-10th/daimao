server {
    listen 8088;
    server_name _;

    root /var/www/daimao-admin/current;
    index index.html;
    client_max_body_size 10m;

    access_log /var/log/nginx/daimao-admin-8088.access.log;
    error_log /var/log/nginx/daimao-admin-8088.error.log;

    location ~ ^/api/(login|admin|upload)$ {
        proxy_pass http://127.0.0.1:8090$request_uri;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|webp|woff2?)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
